# Python Script Principles

## Script Design Rules
Use these rules when generating Python scripts that are more than basic templates.

- Include a clear `main()` entry point.
- Use argument parsing (`argparse`) for non-trivial scripts.
- Add input validation and error handling.
- Keep functions small and testable.
- Prefer standard library first.
