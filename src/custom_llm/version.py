CLI_UI_VERSION = "0.0.23"

# Web UI version is tracked separately from CLI UI and model versions.
WEB_UI_VERSION = "0.0.3"

# Model version is tracked separately from CLI UI version.
LLM_MODEL_VERSION = "0.0.16"

# Backward-compatible alias for older references.
VERSION = CLI_UI_VERSION
